digraph {
	node [shape=circle]
	ROOT -> have
	models -> Such
	have -> models
	advantage -> the
	have -> advantage
	express -> that
	express -> they
	express -> can
	have -> express
	certainty -> the
	certainty -> relative
	express -> certainty
	answers -> of
	answers -> many
	answers -> different
	answers -> possible
	certainty -> answers
	answers -> rather
	rather -> than
	one -> only
	answers -> one
	have -> ","
	have -> producing
	results -> more
	results -> reliable
	producing -> results
	included -> when
	model -> such
	model -> a
	included -> model
	included -> is
	producing -> included
	component -> as
	component -> a
	included -> component
	system -> of
	system -> a
	system -> larger
	component -> system
	have -> "."
}
